{
  "name": "moneybag-api-worker",
  "version": "2.0.0",
  "private": true,
  "description": "MoneyBag Admin API — Cloudflare Worker (Hono.js + D1) — hardened v2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.42.0"
  }
}
